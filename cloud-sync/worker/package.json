{
  "name": "qin-cloud-sync-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler dev -c wrangler.test.jsonc"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.7",
    "vitest": "^4.1.11",
    "wrangler": "^4.131.0"
  }
}
