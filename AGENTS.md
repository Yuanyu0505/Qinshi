# AGENTS.md

本文件用于约定 AI 编程助手在当前 `deepseek` 项目中的工作方式。请在执行任务前阅读并遵守以下规则。

## 1. 沟通规则

- 与用户沟通、解释原因、汇报结果时，默认使用中文。
- 代码、命令、变量名、错误信息、日志和堆栈信息保留英文原文。
- 说明问题时使用清晰、直接的表达；技术细节保持准确。

## 2. 代码修改原则

- 聚焦当前任务，优先解决明确的问题，不提前加入无关功能或过度抽象。
- 尽量保持改动范围小、影响面清晰；问题复杂时再拆分模块处理。
- 每次修改后说明改动内容和影响范围。
- 以可维护性为目标，避免为了增加代码量而增加复杂度。

## 3. 安全要求

- 不得将密码、密钥、API key、token 等敏感信息写入代码。
- 不得提交 `.env` 等本地配置文件。
- 日志中不得输出隐私信息或敏感信息。
- 执行以下高风险操作前，必须先获得用户明确确认：删除文件或批量删除、删除数据库、修改外部接口、上传数据、发送生产请求、操作真实账号、生产环境或第三方平台账号。
- 如果任务涉及不可逆操作，应先说明风险和影响范围，再继续执行。

## 4. 工作习惯

- 项目的功能验证、测试、lint、格式检查及手动验收由用户自行执行；AI 编程助手不主动执行验证流程。
- 修改完成后，说明改动内容、影响范围，并列出建议用户手动验证的重点，不宣称未经用户验证的结果已经通过。
- 当问题存在明确推荐方案时，直接按照推荐方案推进，无需重复向用户确认。
- 只有在存在会影响需求边界、实现结果、安全性或不可逆操作的不确定点时，才向用户提问。
- 安全、低风险的小任务可以直接执行，无需反复确认。
- 遇到项目中已有的工具、脚本、规范或 Skill，应优先复用。
- 不做与当前任务无关的发散修改。
- 涉及删除、密钥、上传、外部账号或生产环境的操作，必须先确认再执行。
