name: Deploy Qinshi PWA to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Prepare static site
        shell: bash
        run: |
          mkdir -p _site/js
          cp index.html manifest.webmanifest service-worker.js .nojekyll _site/
          cp js/ui-performance.js _site/js/
          cp -R css data images icons _site/
          cp js/app.js js/atlas.js js/drops.js js/forging.js js/forbidden.js js/forbidden-ui.js js/inscription-performance.js js/inscription.js js/progress.js js/pwa.js js/query.js js/quiz.js js/settings.js js/tactics.js js/tactics-ui.js js/formations.js js/formations-ui.js js/machine-beasts.js js/machine-beast-school-planner.js js/machine-beasts-ui.js js/battle-box-pill-pouch.js js/battle-box-pill-pouch-ui.js js/equipment-compare.js js/equipment-forging.js _site/js/

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
